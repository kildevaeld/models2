package models;

import "./person";

@doc("This is the address")
record Address {
    @doc("This should stil be here")
    street: string?;
    city: string;
    isoCode: string;
    createdAt: date;
    email: [string];
    test: [model.Test]?;
}
