
import Foundation

{{#each records ~}}
class {{capitalize name}}{{#if extends~}}: {{extends}}{{~/if}} {
{{#each properties}}
    {{#if comment}}//{{comment}}{{/if}}
    {{{property}}}
{{/each}}

{{#each constructors}}
    init({{arguments}}) {
    {{#each initializers}}
        {{{this}}}
    {{/each}}
    }
{{/each}}

}
{{~/each}}